// 默认菜单
import defaultSidebar from './default';
// 映射关系
export default {
    // platform: platformSidebar,
    // 'app-center': appCenterSidebar,
    // 'run-center': runCenterSidebar,
    // 'llm-app': llmManage,
    // 'resource-center': resourceCenterSidebar,
    default: defaultSidebar
};